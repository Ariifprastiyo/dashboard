<section class="section">
  <div class="row">
    <div class="col-lg-12">
      <h1>Social Media Accounts - <%= SocialMediaAccount.count %></h1>

      <div class="card">
        <div class="card-body">
          <h5 class="card-title">Search</h5>
          <%= render 'search_form' %>
        </div>
      </div>

      <%= render 'social_media_accounts_table' %>
      <%= paginate @social_media_accounts, theme: 'bootstrap-5'%>
    </div>
  </div>
</div>

