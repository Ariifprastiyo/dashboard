<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chrome Extension Popup</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
      body {
        width: 350px;
        padding: 15px;
        max-height: 100px;
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      }
      .header {
        display: flex;
        align-items: center;
        margin-bottom: 15px;
        border-bottom: 1px solid #e0e0e0;
        padding-bottom: 10px;
      }
      .logo {
        width: 24px;
        height: 24px;
        margin-right: 10px;
      }
      .brand-name {
        font-size: 18px;
        font-weight: 600;
        color: #333;
      }
      /* Updated styles for flash messages */
      .alert {
        padding: 12px 15px;
        border-radius: 4px;
        margin-bottom: 15px;
        font-size: 14px;
        font-weight: 500;
      }
      .alert-dismissible {
        padding-right: 35px;
      }
      .alert-notice, .alert-success {
        background-color: #d4edda;
        color: #155724;
        border: 1px solid #c3e6cb;
      }
      .alert-alert, .alert-danger {
        background-color: #f8d7da;
        color: #721c24;
        border: 1px solid #f5c6cb;
      }
      .alert-info {
        background-color: #cce5ff;
        color: #004085;
        border: 1px solid #b8daff;
      }
      .alert-warning {
        background-color: #fff3cd;
        color: #856404;
        border: 1px solid #ffeeba;
      }
      .alert ul {
        margin-bottom: 0;
        padding-left: 20px;
      }
      .alert li {
        margin-bottom: 5px;
      }
      .alert li:last-child {
        margin-bottom: 0;
      }
    </style>

  </head>

  <body>
    <div class="container-fluid p-0">
      <div class="header">
        <img src="<%= asset_path('logos/Mediarumu-02.png') %>" alt="Logo" class="logo">
        <span class="brand-name">MediaRumu Extension</span>
      </div>
      <%= render "layouts/flash" %>
      <%= yield %>
    </div>
    
    <div class="environment-badge">
      <%= Rails.env.capitalize %>
    </div>

    <style>
      .environment-badge {
        position: fixed;
        bottom: 5px;
        right: 5px;
        background-color: #f8f9fa;
        color: #6c757d;
        padding: 2px 5px;
        border-radius: 3px;
        font-size: 10px;
        opacity: 0.7;
      }
    </style>
    <!-- Bootstrap JS and Popper.js -->
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.min.js"></script>

    <script>
      // JavaScript to handle closing flash messages
      document.addEventListener('DOMContentLoaded', function() {
        var alerts = document.querySelectorAll('.alert');
        alerts.forEach(function(alert) {
          var closeButton = alert.querySelector('.close');
          if (closeButton) {
            closeButton.addEventListener('click', function() {
              alert.style.display = 'none';
            });
          }
        });
      });
    </script>
  </body>
</html>
