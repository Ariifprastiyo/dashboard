<%= bootstrap_form_for(@bulk_influencer, layout: :horizontal, label_col: "col-sm-3", control_col: "col-sm-9", html: { autocomplete: "off" }) do |f| %>
  <%= f.file_field :bulk_influencer_file %>
  <%= f.primary %>
<% end %>
